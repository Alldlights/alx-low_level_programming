#include <stdio.h>

/**
 * main - Entry point to program
 * @argc: argument count
 * @argv: argument vector
 * Return: on success 0
 */
int main(int argc, char **argv)
{
	printf("%s\n" argv[0]);

	return (0);
}
