#include <stdio.h>
/**
 * main - main block
 * Description: Prints all single digit number of base 10 from 0 in a new line.
 * Return: 0
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		printf("%i", i);
	printf("\n");

	return (0);
}
